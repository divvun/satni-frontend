import React, { Component } from 'react';
import ReactHtmlParser from 'react-html-parser';
import {select} from 'soupselect';

export default class Paradigm extends Component {
  render () {
    return (
      <div>
        huff
      </div>
    );
  }
}
